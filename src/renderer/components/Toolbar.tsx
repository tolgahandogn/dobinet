const Toolbar = () => {
  return (
    <div className="toolbar">
      <button>Yeni (Ctrl+N)</button>
      <button>Düzenle</button>
      <button>Sil</button>
      <button>Dışa Aktar (Ctrl+E)</button>
      <button>Yenile</button>
    </div>
  );
};

export default Toolbar;
